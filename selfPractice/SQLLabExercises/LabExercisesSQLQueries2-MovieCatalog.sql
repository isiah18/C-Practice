USE MovieCatalogGO-- We are moving our G movies to a kids website. Delete all the G rated movies.SELECT * FROM MovieDELETE Movie 	WHERE Rating = 'G'-- Add a new integer field ReleaseYear to the movie table.ALTER TABLE Movie	ADD ReleaseYear intSELECT *FROM Movie--Rename the Runtime column to RuntimeMinutes.EXEC sp_rename 'Movie.RunningTime', 'RuntimeMinutes'SELECT *FROM Movie